# Even Odd Checker in Java

A simple Java program to check whether a given number is even or odd.

## How It Works

The program uses the modulo (`%`) operator.

- If `number % 2 == 0`, the number is even.
- Otherwise, the number is odd.

## How to Run

Compile the program:

```bash
javac EvenOdd.java